<!--%= link_to gravatar_for(current_user, size: 50), current_user %-->
<h1><%= current_user.name %></h1>
<span><%= link_to "个人中心", current_user %></span>
<span><%= pluralize(current_user.zhitiaos.count, "纸条") %></span>